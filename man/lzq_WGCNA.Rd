% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lzq_WGCNA.R
\name{lzq_WGCNA}
\alias{lzq_WGCNA}
\title{Perform WGCNA analysis}
\usage{
lzq_WGCNA(
  exp,
  pheno,
  net.type = "signed",
  powers = c(c(1:10), seq(from = 12, to = 20, by = 2)),
  max.block.size = ncol(exp),
  min.module.size = 30,
  merge.cut.height = 0.25,
  cor.heatmap.x.lab.angle = 45,
  cor.heatmap.sig = "text",
  to.cytoscape = F,
  plot = T
)
}
\arguments{
\item{exp}{Expression matrix with samples as rows and genes as columns}

\item{pheno}{Phenotype data frame with samples as rows and traits as columns}

\item{net.type}{Type of network to construct ("signed", "unsigned", "signed hybrid"). Default is "signed".}

\item{powers}{Vector of soft-thresholding powers to test. Default is c(c(1:10), seq(from = 12, to = 20, by = 2)).}

\item{max.block.size}{Maximum block size for network construction. Default is ncol(exp).}

\item{min.module.size}{Minimum number of genes in a module. Default is 30.}

\item{merge.cut.height}{Height threshold for merging similar modules. Default is 0.25.}

\item{cor.heatmap.x.lab.angle}{Angle for x-axis labels in correlation heatmap. Default is 45.}

\item{cor.heatmap.sig}{Significance display method in heatmap ("text" or "stars"). Default is "text".}

\item{to.cytoscape}{Logical, whether to export network for Cytoscape. Default is FALSE.}

\item{plot}{Logical, whether to generate plots. Default is TRUE.}
}
\value{
A list containing WGCNA results including:
\itemize{
\item modules - Module assignments for each gene
\item MEs - Module eigengenes
\item cor.results - Correlation between modules and traits
\item network - Network adjacency matrix
}
}
\description{
Performs Weighted Gene Co-expression Network Analysis (WGCNA) to identify
modules of co-expressed genes and relate them to external traits. This function
handles the complete WGCNA workflow including network construction, module detection,
and trait correlation analysis.
}
\examples{
\dontrun{
# Basic WGCNA analysis
wgcna_results <- lzq_WGCNA(exp = expression_data, pheno = phenotype_data)

# Customized WGCNA with specific parameters
wgcna_results <- lzq_WGCNA(exp = expression_data, 
                          pheno = phenotype_data,
                          min.module.size = 50,
                          merge.cut.height = 0.15)
}

}
