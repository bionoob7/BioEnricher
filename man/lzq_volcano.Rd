% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lzq_volcano.R
\name{lzq_volcano}
\alias{lzq_volcano}
\title{Volcano plot}
\usage{
lzq_volcano(
  DEG,
  logFC.Ncol = 2,
  Select.P = "FDR",
  P.Ncol = 6,
  DEG.type.Ncol = 8,
  cutoff.P = 0.05,
  cutoff.logFC = 1,
  colors = c("#3E94B5", "#E3E3E3", "#ED6355"),
  color.levels = c("Down", "NoSig", "Up"),
  point.maxsize = 4,
  point.alpha = 0.8,
  intercept.width = 0.65,
  Gene.Ncol = 1,
  Select.genes = NULL,
  label.size = 4,
  legend.position = "bottom",
  theme.plot = theme_bw(base_rect_size = 1.5)
)
}
\arguments{
\item{DEG}{Data frame containing differential expression results}

\item{logFC.Ncol}{Column number containing log fold change values. Default is 2.}

\item{Select.P}{Type of p-value to use ("FDR" or "pvalue"). Default is "FDR".}

\item{P.Ncol}{Column number containing p-values. Default is 6.}

\item{DEG.type.Ncol}{Column number containing differential expression type. Default is 8.}

\item{cutoff.P}{P-value cutoff for significance. Default is 0.05.}

\item{cutoff.logFC}{Log fold change cutoff for significance. Default is 1.}

\item{colors}{Vector of colors for down-regulated, non-significant, and up-regulated genes. Default is c("#3E94B5", "#E3E3E3", "#ED6355").}

\item{color.levels}{Vector of level names corresponding to colors. Default is c("Down", "NoSig", "Up").}

\item{point.maxsize}{Maximum point size. Default is 4.}

\item{point.alpha}{Point transparency. Default is 0.8.}

\item{intercept.width}{Width of significance threshold lines. Default is 0.65.}

\item{Gene.Ncol}{Column number containing gene names. Default is 1.}

\item{Select.genes}{Vector of specific genes to label. Default is NULL.}

\item{label.size}{Size of gene labels. Default is 4.}

\item{legend.position}{Position of legend. Default is "bottom".}

\item{theme.plot}{ggplot2 theme to apply. Default is theme_bw(base_rect_size = 1.5).}
}
\value{
A ggplot2 object representing the volcano plot.
}
\description{
Creates a volcano plot for visualizing differential expression results,
showing the relationship between fold change and statistical significance.
}
\examples{
\dontrun{
# Basic volcano plot
volcano_plot <- lzq_volcano(DEG = my_deg_results)

# Volcano plot with specific genes labeled
volcano_plot <- lzq_volcano(DEG = my_deg_results, 
                           Select.genes = c("TP53", "BRCA1"))
}

}
