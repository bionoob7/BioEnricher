% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lzq_GSEA.R
\name{lzq_GSEA}
\alias{lzq_GSEA}
\title{Gene set enrichment analysis}
\usage{
lzq_GSEA(
  genes,
  gene.type = "SYMBOL",
  enrich.type,
  organism = "Human",
  GO.ont = "BP",
  GO.simplify = T,
  KEGG.use.internal.data = F,
  MsigDB.category = "H",
  CMAP.min.Geneset.Size = 3,
  pvalue.cutoff = 0.05,
  padjust.method = "BH",
  min.Geneset.Size = 10,
  max.Geneset.Size = 1000
)
}
\arguments{
\item{genes}{A named numeric vector of genes with fold changes or scores, where names are gene identifiers}

\item{gene.type}{Type of gene identifiers ("SYMBOL", "ENTREZID", etc.). Default is "SYMBOL".}

\item{enrich.type}{Type of enrichment database to use (e.g., "GO", "KEGG", "WikiPathways", "Reactome", "MsigDB")}

\item{organism}{Organism for analysis ("Human", "Mouse", etc.). Default is "Human".}

\item{GO.ont}{GO ontology type ("BP" for Biological Process, "CC" for Cellular Component, "MF" for Molecular Function). Default is "BP".}

\item{GO.simplify}{Logical, whether to simplify GO results by removing redundant terms. Default is TRUE.}

\item{KEGG.use.internal.data}{Logical, whether to use internal KEGG data. Default is FALSE.}

\item{MsigDB.category}{MsigDB category to use ("H" for hallmark, "C1"-"C7" for other collections). Default is "H".}

\item{CMAP.min.Geneset.Size}{Minimum gene set size for CMAP analysis. Default is 3.}

\item{pvalue.cutoff}{P-value cutoff for significance. Default is 0.05.}

\item{padjust.method}{Method for p-value adjustment ("BH", "bonferroni", etc.). Default is "BH".}

\item{min.Geneset.Size}{Minimum gene set size to consider. Default is 10.}

\item{max.Geneset.Size}{Maximum gene set size to consider. Default is 1000.}
}
\value{
A gseaResult object containing the GSEA results with enriched pathways and their statistics.
}
\description{
Performs Gene Set Enrichment Analysis (GSEA) using various databases including
GO, KEGG, WikiPathways, Reactome, MsigDB, and others. GSEA determines whether
a defined set of genes shows statistically significant differences between
two biological states.
}
\examples{
\dontrun{
# Prepare ranked gene list (fold changes)
gene_list <- c(2.5, 1.8, -1.2, -2.1, 1.5)
names(gene_list) <- c("TP53", "BRCA1", "EGFR", "MYC", "PTEN")
gene_list <- sort(gene_list, decreasing = TRUE)

# GO GSEA analysis
gsea_go <- lzq_GSEA(genes = gene_list, enrich.type = "GO")

# KEGG pathway GSEA
gsea_kegg <- lzq_GSEA(genes = gene_list, enrich.type = "KEGG")

# Hallmark gene sets from MsigDB
gsea_hallmark <- lzq_GSEA(genes = gene_list, enrich.type = "MsigDB", 
                         MsigDB.category = "H")
}

}
