% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lzq_interaction.1toM.plot.R
\name{lzq_interaction.1toM.plot}
\alias{lzq_interaction.1toM.plot}
\title{Plot interaction plot for 1 to M}
\usage{
lzq_interaction.1toM.plot(
  data,
  point.colors = c("#00b4d8", "#e76f51"),
  point.shapes = c("circle", "square"),
  point.sizes = c(15, 10),
  line.width = 4,
  line.color = "#BEBEBE99",
  label.colors = c("black", "black"),
  label.sizes = c(1, 0.8),
  one.circle.layout = F,
  seed = 123
)
}
\arguments{
\item{data}{Data frame with three columns: source, target, and score}

\item{point.colors}{Vector of colors for source and target nodes. Default is c("#00b4d8", "#e76f51").}

\item{point.shapes}{Vector of shapes for source and target nodes. Default is c("circle", "square").}

\item{point.sizes}{Vector of sizes for source and target nodes. Default is c(15, 10).}

\item{line.width}{Base width for connection lines. Default is 4.}

\item{line.color}{Color of connection lines. Default is "#BEBEBE99".}

\item{label.colors}{Vector of colors for node labels. Default is c("black", "black").}

\item{label.sizes}{Vector of sizes for node labels. Default is c(1, 0.8).}

\item{one.circle.layout}{Logical, whether to use circular layout. Default is FALSE.}

\item{seed}{Random seed for reproducible layout. Default is 123.}
}
\value{
A ggplot2 object representing the interaction network
}
\description{
Creates a network visualization showing interactions between one central node
and multiple connected nodes. Useful for visualizing gene-gene interactions,
protein-protein interactions, or other one-to-many relationships.
}
\examples{
\dontrun{
# Create interaction data
interactions <- data.frame(
  source = rep("TP53", 5),
  target = c("MDM2", "CDKN1A", "BAX", "PUMA", "NOXA"),
  score = c(0.8, 0.9, 0.7, 0.6, 0.5)
)

# Plot interactions
plot <- lzq_interaction.1toM.plot(interactions)
}

}
